apiVersion: apps/v1
kind: Deployment
metadata:
  name: yuvomi
  namespace: norriswu0
spec:
  replicas: 1
  selector:
    matchLabels:
      app: yuvomi
  template:
    metadata:
      labels:
        app: yuvomi
    spec:
      containers:
        - name: yuvomi
          image: ghcr.io/ulsklyc/yuvomi:latest
          ports:
            - containerPort: 3000
          env:
            - name: NODE_ENV
              value: production
            - name: DB_PATH
              value: /data/yuvomi.db
            - name: BACKUP_DIR
              value: /backups
            - name: SESSION_SECURE
              value: "false"
            - name: SESSION_SECRET
              valueFrom:
                secretKeyRef:
                  name: yuvomi-secrets
                  key: SESSION_SECRET
          volumeMounts:
            - name: data
              mountPath: /data
              subPath: data
            - name: data
              mountPath: /backups
              subPath: backups
            - name: data
              mountPath: /app/modules
              subPath: modules
            - name: data
              mountPath: /documents
              subPath: documents
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: yuvomi-data
