apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: norriswu0-db
spec:
  parentRefs:
    - name: norriswu0-gateway
      sectionName: http
  hostnames:
    - blayground.norriswu.me
  rules:
    - backendRefs:
        - name: pgadmin
          port: 80
